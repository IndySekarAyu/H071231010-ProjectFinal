<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:gravity="center"
    android:orientation="vertical">

    <!-- Pizza Logo App -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_logo_splash"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="32dp"
        app:cardCornerRadius="60dp"
        app:cardElevation="12dp">

        <ImageView
            android:id="@+id/img_logo_splash"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/white"
            android:padding="24dp"
            android:src="@drawable/ic_app_logo"
            app:tint="@color/colorPrimary" />

    </androidx.cardview.widget.CardView>

    <!-- App Name -->
    <TextView
        android:id="@+id/tv_app_name_splash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:fontFamily="sans-serif-medium"
        android:text="PizzaApp"
        android:textColor="@android:color/white"
        android:textSize="32sp"
        android:textStyle="bold" />

    <!-- App Tagline -->
    <TextView
        android:id="@+id/tv_tagline_splash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:fontFamily="sans-serif"
        android:text="Pesan Pizza Favorit Anda"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:alpha="0.9" />

    <!-- Loading Animation -->
    <ProgressBar
        android:id="@+id/progress_loading_splash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:indeterminateTint="@android:color/white" />

    <!-- Loading Text -->
    <TextView
        android:id="@+id/tv_loading_splash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:fontFamily="sans-serif"
        android:text="Menyiapkan menu pizza..."
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:alpha="0.8" />



</LinearLayout>